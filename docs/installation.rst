.. _Installation:

Installation
************

Install `h3ppy` by using `pip`:

.. code-block:: shell

    pip install h3ppy

And update to the the latest version by

.. code-block:: shell

    pip install h3ppy --upgrade

