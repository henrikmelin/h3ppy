.. _Installation:

Installation
************

Install `h3ppy` by using `pip`:

.. code-block:: shell

    pin install h3ppy

And update to the the latest version by

.. code-block:: shell

    pin install h3ppy --upgrade

